# IMPORTANT: This file is a TEMPLATE only.
# DO NOT apply this file directly to the cluster — it contains placeholder values.
#
# To create real secrets, run:
#   bash scripts/create_k8s_secrets.sh
#
# That script reads from:
#   - AWS SSM Parameter Store (production)
#   - .env file (local development)
#
# ── Reference only — shows the Secret schema ──────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: healthalliance-secrets
type: Opaque
stringData:
  postgres-user: "GENERATED_BY_SCRIPT"
  postgres-password: "GENERATED_BY_SCRIPT"
  database-url: "GENERATED_BY_SCRIPT"
  mlflow-s3-bucket: "GENERATED_BY_SCRIPT"
  api-keys: "GENERATED_BY_SCRIPT"
